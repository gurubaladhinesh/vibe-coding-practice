# Unified GitHub Contribution Graph

Merge contribution calendars from multiple GitHub accounts into one heatmap.

## Run

```bash
cd git-contributions-aggregator
npm install
npm run dev
```

Add GitHub personal access tokens (classic tokens need `read:user`). Tokens are stored in `localStorage` and shown masked.

## Test

```bash
npm test
```
